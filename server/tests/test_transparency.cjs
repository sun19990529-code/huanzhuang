const fs = require('fs');
const path = require('path');

// 纯 JS 简易 PNG / PPM / BMP / 或者利用已有的图片解码方式处理像素
// 或者使用简易纯像素算法读取 jpeg 并生成带 alpha 的 png
console.log('Testing image transparency processing...');
